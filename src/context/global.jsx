import { createContext, useState, useEffect } from "react"

/*Contexto del botón 'Notificación' y 'User' del header*/
export const HeaderContexto = createContext()

/*Contexto para el formulario de usuarios*/
export const UsersContexto  = createContext()